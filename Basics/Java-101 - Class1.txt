--> In java 9 "jshel" has been introduced, it is lke python shell scripting.`We just type jshell in cmd, and tghe cmd enters into a scripting mode, where we can simply add w onumbers, print etc.
--> Our javacode is independent, but jvm itself is not independent. We have seperate setup for IOS.
--> Java will always start from "main()" method with a particular signature(public static void main(String args[])).
--> JRE is usually present inside JRE. Which will provide the extra library dependencies to JVM while running the code.
--> In my sys, I have writte n a java code and compiled it, i will get a className.class file. Now if we want this file to run in my friends machine, it is ok even if my friend's machine has only JRE and JDK, because JDK will usually have compiler + JRE in it. Since we compiled the java file, we don't need it further.
--> Java is a strongly typed programming language. This means it enforces strict rules regarding data types and does not allow implicit conversions between incompatible types, which helps catch errors at compile time and prevents many runtime issues.
--> When saving a number in long, we should always end the number with l. Eg: long a = 200l
--> When you get confused about no.of zeroes in a number, in java, we can seperate them using "_" E: int a = 100_00_00_000
--> Post increment Vs Pre increment - Both does the same, but when we assign the operation's value to a variable, then we will find the difference. int b = ++a, will first increment a and assign the incremented value to b.int b = a++, but here first assignement will happen then only increment.
Calc c = new Calc, c is a reference variable. Now the c will be present in stack. When we do new Calc, the obj will be created in heap, with it's reference stored in c(in stack).